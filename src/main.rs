mod lib;

fn main() {
    // let scene = lib::test_scene(800, 600);
    // // Create a new ImgBuf with width: imgx and height: imgy
    // let mut imgbuf = image::ImageBuffer::new(scene.width as u32, scene.height as u32);
    // let pixels = (1..scene.width).flat_map(|x| (1..scene.height).map(move |y| (x, y)));
    // for (x, y) in pixels {
    //     let color = scene.render_pixel(x, y);
    //     let pixel = imgbuf.get_pixel_mut(x as u32, y as u32);
    //     *pixel = image::Rgb([color.r, color.g, color.b])
    // }

    // imgbuf.save("render.png").unwrap();
}
